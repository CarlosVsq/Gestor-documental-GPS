/**
 * Enum de Roles del Sistema — HU-25
 * Define los roles disponibles para los usuarios.
 * Por ahora solo ADMIN; se extenderá con más roles en el futuro.
 */
export enum Role {
  ADMIN = 'admin',
  EDITOR = 'editor',
}
